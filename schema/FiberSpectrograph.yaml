$schema: http://json-schema.org/draft-07/schema#
$id: https://github.com/lsst-ts/ts_FiberSpectrograph/blob/master/schema/fiberspectrograph.yaml
# title must end with one or more spaces followed by the schema version, which must begin with "v"
title: FiberSpectrograph v1
description: Schema for FiberSpectrograph configuration files
type: object
properties:
 fitsrfilepath:
    description: relative path of folder on disk where fits file will be stored relative to the root of module
    type: string
    default: "fits/data"
required:
  - fitsrfilepath
additionalProperties: false
